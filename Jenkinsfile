pipeline {
    agent any

    stages('CI') {
       stage(''){
            steps {
                checkout scm
            }
        }

       stage('build'){
            steps {
                echo "Build"
            }
        }

        stage('Test'){
            steps {
                echo "Test"
            }
        }

    }
}