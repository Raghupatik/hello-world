pipeline {
    agent any

    stages('CI') {
       stage(''){
            steps {
                checkout scm
            }
        }

    stages('build') {
       stage(''){
            steps {
                echo "Build"
            }
        }
        
    stages('Test') {
       stage(''){
            steps {
                echo "Test"
            }
        }

    }
}